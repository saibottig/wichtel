{
  "name": "wichtel",
  "private": true,
  "type": "module",
  "description": "Wichtel-Auslosung: ein Buchstabe und eine Farbe pro Jahr.",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "archivieren": "node scripts/archivieren.mjs",
    "three-holen": "node scripts/three-holen.mjs"
  },
  "devDependencies": {
    "playwright": "^1.63.0"
  }
}
